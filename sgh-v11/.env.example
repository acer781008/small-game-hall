# 主控登入密碼（正式部署請在 Render Environment 設定）
ADMIN_PASSWORD=請改成你自己的主控密碼

# 永久資料庫連線字串。Render PostgreSQL / Neon / Supabase 等 PostgreSQL 都可。
# 正式 Render 部署若要永久保留活動、獎品、得獎紀錄，請務必設定。
DATABASE_URL=

# 某些本機 PostgreSQL 不使用 SSL 時可設 disable；雲端 PostgreSQL 通常不用填。
# DATABASE_SSL=disable
